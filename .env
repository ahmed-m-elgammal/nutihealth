# Public Expo environment variables (safe to ship in client bundles)
# Keep real API secrets in backend/.env only.

# OpenFoodFacts doesn't require an API key, but user agent helps with attribution.
EXPO_PUBLIC_OPENFOODFACTS_USER_AGENT=NutriHealth/1.0.0

# Backend API base URL for proxy routes used by the mobile app.
EXPO_PUBLIC_API_URL=http://localhost:3000/api
